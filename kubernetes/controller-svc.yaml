apiVersion: v1beta3
kind: Service
metadata:
  labels:
    name: controller
  name: controller
spec:
  ports:
  - name: mysql
    port: 3306
  - name: ldap
    port: 389
  selector:
    name: controller
